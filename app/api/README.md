// app/api/README.md
// ================================================================
// API Routes — Next.js Route Handlers
// ================================================================
//
// Add server-side API endpoints here as needed. Examples:
//
//   app/api/verify-token/route.js   → Verify Firebase ID token (Admin SDK)
//   app/api/send-email/route.js     → Send transactional emails
//   app/api/export-pdf/route.js     → Generate and stream PDFs
//
// Each route file must export named handlers:
//   export async function GET(request) { ... }
//   export async function POST(request) { ... }
//
// Use the Firebase Admin SDK from @/src/lib/firebase-admin for
// server-side Firestore reads and token verification.
// ================================================================
